#include <stdio.h>

int main(void) { // write void
  int sum; // write int
  /* COMPUTE RESULT */ // close comment, fairly obvious comment

  sum = 25 + 37 - 19; // don’t forget the semicolon
  printf("The answer is %i\n", sum); // don’t forget the comma
  return 0;
}
